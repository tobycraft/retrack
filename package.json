{
  "name": "paste-minimal-changes-tests",
  "private": true,
  "description": "Dev-only test tooling for the Paste Minimal Changes Word Add-in. Not part of the deployed docs/ site.",
  "scripts": {
    "test:e2e": "playwright test",
    "validate": "office-addin-manifest validate manifest.xml"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "office-addin-manifest": "^2.1.6"
  }
}
